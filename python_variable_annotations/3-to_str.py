#!/usr/bin/env python3
"""Script that receives a float as an argument and returns it as a string"""


def to_str(n: float) -> str:
    """Function that returns a string."""
    string = str(n)
    return(string)
